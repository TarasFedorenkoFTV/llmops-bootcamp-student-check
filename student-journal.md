# Журнал студента · LLMOps Bootcamp · прогін №2

Профіль: senior backend (Python), C# читаю але не пишу, з LLM працював як зі звичайним API.
Робочий репозиторій: `C:\Work\llmops-student` (створений зі `starter/` за GETTING_STARTED §11 крок 0).
Матеріали курсу: `C:\Work\llmops-bootcamp` (не редагую).

**Контекст прогону.** Це повторне проходження: перший прогін відбувся 2026-08-04,
після нього автор оновив матеріали (коміт «ДЗ і гайди: негативні перевірки в кожному
тижні, guardrail у core hw4, робочі інструменти» + правки лонгрідів). Тому цей журнал —
одночасно і проходження, і **ре-тест дефектів першого прогону**: для кожного
перевіряю — виправлено / не виправлено / виправлено з новим дефектом.

Оточення: Windows 11, Docker 29.5.3 / Compose v5.1.4, Git Bash. Зайняті порти цього
разу: 8080+5432 (EDB Apache, PostgreSQL 18 — як і раніше) **плюс 18080/15432/4200/4000 —
на машині паралельно живе стек ментора `solutions-*`**. Мої порти: service **28080**,
postgres **25432**, ui 24200, gateway 24000.

---

## Тиждень 0 · старт (крок 0, порти, стек)

- **що робив:** крок 0 за §11; порти за новим `docker-compose.override.yml.example`;
  `docker compose up --build`.
- **що вийшло:**
  ```
  $ git ls-files | wc -l
  44          # було 41: +.gitignore, +demo-ports.yml, +docker-compose.override.yml.example
  ```
  Стек піднявся з першої спроби, по одному мапінгу на сервіс, gateway був готовий
  одразу (образи в кеші з прогону №1).

- **ре-тест дефектів прогону №1:**

  | Дефект №1-прогону | Стан |
  |---|---|
  | `starter/` без `.gitignore`, ключ поїде в публічний репо | **ВИПРАВЛЕНО** — `.gitignore` у стартері, включно з `docker-compose.override.yml` |
  | «перемап порт» без механізму (`!override`-пастка) | **ВИПРАВЛЕНО** — `docker-compose.override.yml.example` з поясненням про злиття list-полів; згадано, що CI очікує стандартні порти |
  | index.html без посилань на уроки | **ВИПРАВЛЕНО** — `href="L01.html"…L12` на місці |

- **де спіткнувся (нове):** **`demo-ports.yml` і `override.example` конфліктують між
  собою по postgres.** Обидва файли маплять postgres на **15432**. `demo-ports.yml`
  прямо заявляє призначення «щоб стек піднімався паралельно з іншим» — але якщо
  «інший» стек піднятий за `override.example` (мій випадок: поруч живе стек ментора),
  паралельний запуск падає рівно на postgres:
  ```
  Error ... Bind for 0.0.0.0:15432 failed: port is already allocated
  ```
  Сервісні порти в них розведені (8180 vs 18080), а postgres — ні. Виглядає як
  недогляд при додаванні двох файлів одним комітом. Обійшов третім зсувом
  (25432/28080/24200/24000) у своєму override.
- **скільки заходів знадобилось:** стек — 2 (перший up упав на 15432).
- **чи дивився еталон:** ні.
- **суб'єктивно:** старт тепер робиться за 10 хвилин проти вечора в прогоні №1.
  Найдорожчі граблі (gitignore, !override, посилання) прибрані.

---

## Тиждень 1 · L01 + L02 + hw1

- **що робив:** лаба L01 (обидві половини, лог, маркери зламу), hw1 повністю
  (seed, `GetActivePrompt`, `GET /prompts`, `POST /prompts/{v}/activate`,
  `prompt_version` у лог), прогін evals у два боки.
- **що вийшло:**
  ```
  чат:        [14ms] Щоб скинути пароль: відкрийте сторінку входу…
  __fail_503: [41ms] Сервіс тимчасово недоступний.     -> status 503 у лозі
  __fail_429: [31ms] Сервіс тимчасово недоступний.     -> status 429
  __garbage:  [12ms] ...                               -> status 200 (!) — «200 і сміття» наживо

  реєстр:   v1 f / v2 t (з міграції)
  rollback: activate v1 -> «не знаю»; v99 -> HTTP 404, active не злетіла; v2 -> полагодилось
  лог:      mock | v2 | 200  /  mock | v1 | 200
  evals:    v1 -> 3/6 (червоно), v2 -> 6/6 (зелено)
  ```

- **ре-тест дефектів прогону №1:**

  | Дефект | Стан |
  |---|---|
  | Команда прогону evals не працює в Git Bash (блокер №1) | **ВИПРАВЛЕНО** — §6 тепер дає `MSYS_NO_PATHCONV=1` + `$(pwd -W)`; перевірив дослівно: `eval: 6/6 passed`. Діагноз у тексті теж виправлений (названо і зламаний аргумент, і тихий порожній маунт) |
  | L02 лаба: «SQL — у блоці 04» (битий крос-реф) | **ВИПРАВЛЕНО** — тепер «блок 06» |
  | L01: «перевірите обробку finish_reason на стартері» — нема чого перевіряти | **ВИПРАВЛЕНО** (формулювання з обіцянкою зникло з L01; поле живе в опційній частині hw1 — чесно) |

- **де спіткнувся:** цього разу — ніде. Той самий обсяг роботи (≈60 рядків C#),
  той самий один захід на код.
- **чи дивився еталон:** ні.

---

## Тиждень 2 · L03 + L04 + hw2 (routing + cost)

- **що робив:** `Route()` (одна точка рішення), прайс, `cost_usd`, `GET /cost`, fallback-порядок як політика.
- **що вийшло:**
  ```
  розподіл: mock-mini 3 / mock-strong 2 — трафік розведений
  вартість: mock-strong SUM 0.000272 проти mock-mini 0.000046 (≈6× на цих кейсах)
  GET /cost: {"today_usd":0.0003,"budget_usd":5.0}
  ```
- **ре-тест дефектів прогону №1:**

  | Дефект №1 | Стан |
  |---|---|
  | Плитка вартості завжди `$0.00` (готовий `toFixed(2)`) | **ЧАСТКОВО** — код консолі той самий (`toFixed(2)`, `$0.00`), але L04 блок 07 тепер **чесно попереджає**: «готова консоль округлює до центів — тож на mock плитка показує $0.00 / $5.0». Тобто дефект коду лишився, але хибний висновок студента знято текстом. Прийнятно. |
  | `GROUP BY request_id` заборонений схемою (PK) | **ВИПРАВЛЕНО в тексті** — L04 більше не каже «заведіть звичку зараз»; тепер: «принцип запам'ятайте зараз, застосуєте при першій появі другого виклику». Схема та сама (`request_id PRIMARY KEY`), але суперечність «роби зараз» знята. |
  | Перемикач `MODEL` ламає облік (немає ціни для gpt-4o-mini) | **не перевіряв** (лишаюсь на mock; це проявляється лише з реальним ключем). Прайс і далі без реальних моделей. |

- **де спіткнувся:** нічого нового. hw2 і далі — найлегше ДЗ за кодом (усе тіло в уроці готове), калібровка «складне з тижня 2» і далі не тримається.
- **чи дивився еталон:** ні.

---

## Тиждень 3 · L05 + L06 + hw3 (кеш + інструменти)

- **що робив:** кеш (ключ = модель+промпт+повідомлення), лічильники hit/miss, `Tools.Run` із таймаутом 3с та ідемпотентністю по `request_id`, колонки `cache_hit`/`tool` у схемі.
- **що вийшло:**
  ```
  кеш:        1-й [15ms] miss, 2-й [0ms] hit, 3-й [0ms] hit
  інструмент: [16ms tool=lookup_order] «…(статус: оплачено, доставку призначено)»
  промпт у ключі: activate v1 -> «не знаю» (кеш НЕ віддав стару відповідь)
  лог: cache_hit=t на повторах з 0 токенів; tool=lookup_order записаний
  evals: 6/6
  ```
- **ре-тест дефектів прогону №1:**

  | Дефект №1 | Стан |
  |---|---|
  | `request_id` як ключ ідемпотентності не розв'язує свій сценарій | **ВИПРАВЛЕНО в тексті** — L06 тепер прямо каже: «`request_id` закриває лише ваш ретрай у межах запиту; повтор моделі наступним ходом чи подвійний клік — нові `request_id`; від них захищає лише `hash(conversation_id, tool_name, args)`». hw3 п.3 повторює цю межу дослівно. Найкраще виправлення з усіх — помилку в змісті замінили точним формулюванням. |
  | `__delay` не перевіряє tool-таймаут | **ВИПРАВЛЕНО в тексті** — hw3 опційне тепер: «`__delay` затримує mock, тобто виклик моделі, а не інструмент — tool-таймаут ним не перевіряється; перевіряй юніт-тестом». |
  | Схема інструментів «нема на чому валідувати» (mock шле `{}`) | **визнано** — hw3 опційне: «mock шле порожні `arguments` — кейс симулюється юніт-тестом». Обов'язкова доріжка валідацію не вимагає, тому обов'язковий крок лаби більше з нею не конфліктує. |

- **де спіткнувся (нове, інструментальне — не про курс):**
  Мій власний скрипт правок через `python … .replace()` **двічі мовчки не застосувався**
  до `Program.cs`/`schema.sql`, і я витратив захід на діагностику. Причини дві, обидві
  мої, не курсу: (1) один Bash-виклик із Python+docker був заблокований, коли класифікатор
  безпеки був недоступний — Python не виконався взагалі, але наступний крок (rebuild) я
  запустив, вважаючи, що правки на місці; (2) exact-match `.replace()` крихкий до найменшої
  розбіжності. Урок для себе: правки коду — через Edit-інструмент (він толерантний до
  дрібниць), а не через generovану Python-заміну; і не запускати rebuild, не переконавшись,
  що попередній крок реально відпрацював. Дефекти W3 при цьому підтвердилися вже на
  виправленому коді.
- **чи дивився еталон:** ні.

---

## Тиждень 4 · L07 + L08 + hw4 (надійність + безпека)

- **що робив:** fallback-ланцюг (`CallGateway`, збій=значення), circuit breaker per-model із half-open, graceful degradation, HITL-черга (approve/reject атомарно), guardrails (PII-маскування + детект injection), 4 red-team кейси в датасет, `mock-dead` у litellm-config.
- **що вийшло:**
  ```
  __fail_503:      [63ms] ввічлива заглушка, status 503 (не 500)
  __outage:        [19ms] fallback відпрацював, у лозі model=mock-mini status=200 fallback_used=t
  injection ×3:    усі три родини -> «не можу виконати», blocked=injection у лозі
  нормальні:       НЕ блокуються (перевірив 4 контрольні)
  PII:             мій email taras@... замаскований до відправки
  HITL:            заявка -> approve -> #T-b5c5; повторний approve -> 404
  5 одночасних approve: рівно 1 тікет #T-db36 (check-then-act через TryUpdate)
  reject:          {"result":"rejected"}
  circuit breaker: 3 збої -> open; здорове питання [0ms] заглушка (САМОСТРІЛ);
                   через 6с half-open -> [11ms] нормальна відповідь
  evals:           10/10 (6 базових + 3 injection + подяка), поріг 9
  ```
- **ре-тест дефектів прогону №1:**

  | Дефект №1 | Стан |
  |---|---|
  | Крок 4 лаби L08 недосяжний (mock ловить лише англ.) | **ВИПРАВЛЕНО як пункт ДЗ** — hw4 опційне тепер прямо називає: «Вхідний regex-детект prompt injection (три родини патернів з уроку 8) на межі сервісу: без нього перефразовані атаки в eval-датасеті не ловляться». Тобто робота, яку в прогоні №1 я робив «поза ДЗ», тепер названа й обґрунтована. Реалізував — 3 родини блокуються. |
  | Успішний fallback на mock не побачити (три документи різне) | **ЧАСТКОВО** — hw4 і далі каже «на mock обидві спроби падають, дивимось код», `mock-dead` у стартер не додали. Я знову додав його сам (як у прогоні №1) — але тепер це моя надбудова, а не латка суперечності: текст ДЗ принаймні узгоджений сам із собою (не обіцяє «fallback відпрацював» там, де його не видно). |
  | `status == 0` не дає чесний 503 у деградації | **ВИПРАВЛЕНО** — L07 тепер `(status == 200 \|\| status == 0) ? 503 : status`. |
  | Заявка approvals без контексту й без reject у консолі | **ЧАСТКОВО** — ендпоінт я віддаю з контекстом (message, request_id, waiting_sec) і зробив `reject`. Але це API; чи готова консоль малює reject-кнопку — перевірю на W5 разом з усіма плитками. |
  | HITL: «рівно один тікет» неможливо перевірити (немає сліду) | **ВИПРАВЛЕНО** — hw4 п.2 тепер вимагає «спостережуваний слід: після approve — `result` у заявці». Мій `/approvals` це віддає. |
  | guardrail PII в обов'язковій доріжці? | **ВИПРАВЛЕНО** — hw4 п.3 тепер робить `MaskPii` **обов'язковим** (у прогоні №1 було опційним). |

- **де спіткнувся:** нове — CS8803 (локальна функція `CallGateway` мусить стояти до
  оголошень типів). У прогоні №1 воно теж було; §9a гайду досі це не покриває, але я
  вже знав і одразу поставив функцію перед `record`. Тобто це стабільний C#-бар'єр
  для пітоніста, який курс так і не закрив.
- **чи дивився еталон:** ні.
- **суб'єктивно:** найважчий тиждень, і найбільше виправлень із прогону №1 саме тут —
  автор явно читав рольове рев'ю (guardrail і слід approve стали обов'язковими,
  injection-детект названо). Найкраще виправлення — не код, а те, що ДЗ тепер
  узгоджене саме з собою.

---

## Тиждень 5 · L09 + L10 + hw5 (observability + evals)

- **що робив:** `GET /observability` (p95/requests/error_rate/cache_hit/fallback — усе з лога), `GET /providers` (джерело — стан breaker'а), 4 red-team кейси (зроблено на W4), перевірка на фальш-позитиви, поріг+SLO у README.
- **що вийшло:**
  ```
  /observability: {"p95_ms":225,"requests":63,"cache_hit_pct":11.1,"error_rate_pct":12.7,"fallback_events":7}
  /providers:     mock-mini · down (fails=3) · mock-strong · ok
  звірка:         requests-плитка 63 == SELECT count(*) 63
  консоль:        усі 6 плиток із числами; картка провайдерів жива
  outage-прогін:  4/10 — і всі 4 це expect_refusal (guardrail на межі), 0 expect-кейсів
  ```
- **ре-тест дефектів прогону №1:**

  | Дефект №1 | Стан |
  |---|---|
  | hw5 п.4 суперечить L08 (0 пройдених vs guardrail на межі) | **ВИПРАВЛЕНО** — hw5 п.4 тепер має явний виняток: «якщо зробив вхідний injection-детект, `expect_refusal`-кейси залишаться зеленими і під час збою — бо відмова народжується до виклику моделі, і це правильніше». Мій прогін дав рівно 4 зелені refusal — тепер це очікувано, а не «на доопрацювання». |
  | SQL без `COALESCE` (виняток на порожній базі) | **ВИПРАВЛЕНО** — L09 тепер дає `COALESCE` (3 згадки в уроці). |
  | grader у L10 з `"вибачте"` (той самий баг двома блоками вище) | **ВИПРАВЛЕНО** — L10 блок 05 тепер без `"вибачте"`, з коментарем чому. |
  | лічильники в пам'яті проти «переселити в базу» | **обійшов як і раніше** — беру cache_hit/fallback з колонок лога, не з пам'яті. Урок і далі дає гібрид у блоці 04, але тепер це «на подумати», а не суперечність (перевіряти нема потреби). |
  | статус провайдера чесний у core (без breaker) | hw5 таблиця тепер приймає для core «`degraded` після N поспіль збоїв ланцюга» — тобто визнано, що без опційного breaker плитка теж має оживати. У мене breaker є, тому `down`/`ok` справжні. |

- **де спіткнувся:** нічого нового.
- **чи дивився еталон:** ні.
- **суб'єктивно:** найрівніший тиждень і в прогоні №1, і тепер. Найважливіше
  виправлення — hw5 п.4: суперечність, яка в прогоні №1 змушувала мене вибирати між
  двома ДЗ, тепер знята явним винятком у тексті.

---

## Тиждень 6 · L11 + L12 + hw6 (CI-гейт + фінал)

- **що робив:** поріг гейта 9/10 + `ci/static_checks.py`, реальні PR у GitHub з CI,
  branch protection, демо red→green, INCIDENT_RUNBOOK, інцидент-демо + KPI + 30-day
  rollout у README, наскрізний прогін із чистого клону.
- **що вийшло:**
  ```
  ІСТОРІЯ ACTIONS (доказ hw6 п.2):
    success  capstone            10/10 passed, threshold 9
    failure  demo/broken-prompt   6/10 passed  -> exit 1 -> BLOCKED
    success  demo/broken-prompt  10/10 passed  -> exit 0 -> CLEAN

  merge червоного PR: "the base branch policy prohibits the merge" (main protected, eval required, enforce_admins)
  інцидент-демо: down -> half-open (6с) -> ok; провайдери оживають
  чистий клон: усі 6 кроків наскрізного сценарію + усі ендпоінти живі
  ```
- **ре-тест дефектів прогону №1:**

  | Дефект №1 | Стан |
  |---|---|
  | Порада §8 про перемап порту несумісна з CI-шаблоном (був блокер) | **ВИПРАВЛЕНО** — `docker-compose.override.yml` тепер у `.gitignore` стартера; capstone-CI зелений з першої спроби (у прогоні №1 перший прогін падав на `18080` через закомічений override). |
  | Шаблон гейта з порогом 5 (зелений при катастрофі) | **треба виправити — НЕ виправлено** у стартері (`--threshold 5` і далі приходить), але я оновив під датасет (9/10). Механічної перевірки «поріг ≥ правило(N)» у шаблоні так і немає — я закрив це в `ci/static_checks.py` частково (валідність датасету), але саме зв'язку поріг↔N шаблон не ловить. |
  | L12 просить окремий runbook, hw6 — розділ у README | **лишилось** — зробив обидва (`INCIDENT_RUNBOOK.md` + демо в README). |
  | тиждень 6 неперевірюваний без GitHub | **не про матеріали** — цього разу доступ був, зробив реальні PR і CI. |

- **де спіткнувся:** нічого нового по курсу. Інструментально — inline-Python-принти
  метрик падали на cp1252 без `PYTHONUTF8=1` (кирилиця у форматному рядку); виправив
  `export PYTHONUTF8=1`. До курсу відношення не має.
- **чи дивився еталон:** ні. Живий demo-PR з еталонного репо теж не відкривав.
- **суб'єктивно:** прогін №2 тижня 6 пройшов помітно чистіше за №1 саме тому, що
  головний блокер (override у git) прибрано в стартері — CI позеленів одразу.
