# Журнал студента · LLMOps Bootcamp · прогін №3

Профіль: senior backend (Python), C# читаю але не пишу, з LLM працював як зі звичайним API.
Робочий репозиторій: `C:\Work\llmops-student-run3` (створений зі `starter/` за GETTING_STARTED §12 крок 0).
Матеріали курсу: `C:\Work\llmops-bootcamp` (не редагую).

**Контекст прогону.** Це третє проходження. Після прогону №2 (2026-08-05) автор
виправив знахідки (коміти «Правки за рольовими прогонами №2: 6 підтверджених знахідок»
і «Закриття gap-ів до 5/5»). Цей журнал — і чесне проходження, і ре-тест: чи закриті
дефекти прогону №2, чи не з'явились нові.

Оточення: Windows 11, Docker 29.5.3 / Compose v5.1.4, Git Bash.
Зайняті порти на машині: **8080** (Apache, PID 4812) і **5432** (PostgreSQL, PID 6676) — чужі
локальні сервіси, звільнити не можу. 4200 і 4000 вільні (рядок `:40001` у netstat — «схожий
порт», PREREQUISITES про це чесно попереджає).

---

## Тиждень 0 · index.html + PREREQUISITES + GETTING_STARTED

- **що робив:** прочитав `docs/longreads/index.html`, `starter/PREREQUISITES.md`,
  `starter/GETTING_STARTED.md`. Виконав самоперевірку з PREREQUISITES §3.
  Створив свій репозиторій зі стартера (§12 крок 0): `cp -r starter/. .`,
  `git init -b main`, коміт `62327ff`.
- **що вийшло:**
  - `docker --version` → 29.5.3-rd; `docker compose version` → v5.1.4; git 2.55.0.
  - `netstat` показав LISTENING на 8080 і 5432 → мій кейс із Troubleshooting §8 рядок 1.
  - У стартері тепер є `docker-compose.override.yml.example` і `demo-ports.yml` —
    у прогоні №2 override.example уже був, перевірю чи працює як описано.
- **де спіткнувся:** поки ніде. PREREQUISITES чесно попереджає і про «схожі порти»
  (мій `:40001`), і про підступний зайнятий порт із чужим 404.
- **скільки заходів знадобилось:** 1.
- **чи дивився еталон:** ні.
- **суб'єктивно:** вхід охайний: index → prereqs → гайд читаються за ~40 хв, порядок дій
  однозначний. GitHub-репо відклав до першого PR — гайд каже «один раз», але не каже,
  що пуш потрібен саме зараз; для локальної роботи достатньо git init.

## Тиждень 0 · перший запуск стека

- **що робив:** порти 8080/5432 зайняті → за Troubleshooting §8 скопіював
  `docker-compose.override.yml.example` → `docker-compose.override.yml`. Порти прикладу
  (14200/18080/14000/15432) на цій машині ТЕЖ зайняті (крутиться чужий стек `solutions-*`),
  перемапив на 24200/28080/24000/25432. `docker compose up --build -d`.
- **що вийшло:**
  - усі 5 контейнерів Up; health-«трійка» (localhost / 127.0.0.1 / [::1]) → тричі `{"status":"ok"}`;
  - `POST /chat {"message":"please check my order"}` → `{"request_id":"2a6a…","content":"Перевіряю статус вашого замовлення…","tool":"lookup_order","latency_ms":729}`;
  - UI на :24200 — точка А як в index.html: чат живий, усі плитки консолі «—»;
  - українською через UI-proxy: «Як скинути пароль?» → коректна відповідь про «Забули пароль», latency 16ms.
- **де спіткнувся:** ніде — сценарій «порт зайнятий» повністю покритий override-прикладом
  з інструкцією про `!override` прямо в коментарях файлу. Ре-тест прогону №2: цей шлях
  тепер working out of the box. Дрібниця: приклад містить фіксовані 1xxxx-порти, які теж
  можуть бути зайняті (мій випадок) — але правити їх очевидно, не дефект.
- **скільки заходів знадобилось:** 1 (з урахуванням перемапу — 2 правки override).
- **чи дивився еталон:** ні.
- **суб'єктивно:** перший запуск — найкоротший із трьох прогонів; попередження про
  «схожі порти» і health-трійку зʼявились у матеріалах саме там, де студент спіткнувся б.

---

## Тиждень 1 · L01 (лонгрід + лаба)

- **що робив:** прочитав L01 повністю. Лаба: кроки 1–2 (стек + огляд UI) зроблені ще на
  смоуку; крок 3 — прочитав `service/Program.cs` згори вниз, знайшов усі TODO(student):
  W1 systemPrompt хардкод (рядок 31), W2 routing (28) + cost (87), W3 cache (34) + tools (66),
  W4 guardrails (25) + fallback (37) + graceful degradation (80), W5 ендпоінти консолі (97–101);
  крок 4 — SELECT з requests: мої 2 запити в лозі, порожні provider/prompt_version/cost_usd —
  збігається з блоком 07 уроку; крок 5 — `__fail_503` → «Сервіс тимчасово недоступний.»,
  у лозі status=503, latency 48ms, плану Б нема. Опційний крок 6: `--profile advanced` →
  додався один контейнер redis, погасив.
- **що вийшло:** усе з першого разу. Чек-лист «перевір себе» проходжу: шлях UI→сервіс→
  адаптер→mock→лог намалюю з пам'яті; temperature=0 ≠ детермінізм (батчинг провайдера,
  неасоціативність float, оновлення снапшота — тому перевіряти властивості, не рядки);
  дві невидимі для HTTP-моніторингу категорії — «200 і непридатна відповідь» (length/сміття)
  і «тиха деградація якості».
- **де спіткнувся:** ніде. Дрібне спостереження: лаба каже «Відкрити localhost:4200», а я
  на override-портах маю 24200 — Troubleshooting чесно попереджав «читай усі команди з
  новим портом», тож не дефект, але тримати в голові доводиться постійно.
- **скільки заходів знадобилось:** 1.
- **чи дивився еталон:** ні.
- **суб'єктивно:** найкорисніше для мого профілю — finish_reason і те, що length треба
  трактувати як збій формату; я всі роки читав тільки content. Розділ про C# (§10 гайду)
  зняв половину страху перед Program.cs.

## Тиждень 1 · L02 (лонгрід + лаба) + hw1

- **що робив:** прочитав L02. Лаба: (1) seed v1/v2 у `db/schema.sql` + `down -v && up -d`;
  (2) `GetActivePrompt` у Program.cs замість хардкоду, версія → `LogRequest` →
  `prompt_version`; (3) `GET /prompts` списком + `POST /prompts/{version}/activate`
  з перевіркою існування (404) і атомарним `UPDATE prompts SET active=(version=@v)`;
  (4) регресія наживо. Потім чек-лист ментора з hw1 повністю.
- **що вийшло (фактичні виводи):**
  - `SELECT name, version, active FROM prompts` → 2 рядки, active лише v2;
  - activate v1 → чат «не знаю»; activate v3 (одруківка) → `404 {"error":"unknown version 'v3'"}`, активна не змінилась; rollback v2 → відповіді полагодились;
  - лог: `prompt_version=v2` (потім v1/знову v2 — збігається з активною);
  - поломка реєстру: `UPDATE prompts SET active=false` → чат «не знаю», у лозі `prompt_version=none`;
  - `grep -ri "INSERT INTO prompts" service/` → 0;
  - evals (команда GETTING_STARTED §6, Windows-варіант із `MSYS_NO_PATHCONV=1` + `$(pwd -W)`):
    v2 → `eval: 6/6 passed, threshold 5`, exit 0; v1 → 3/6, exit 1 — точно «еталонна
    картина 3/6» із hw1; rollback → знову 6/6.
  - консоль: картка Prompt registry ожила (v1 ARCHIVED + кнопка, v2 ACTIVE).
- **де спіткнувся:** Windows-рецепт evals із §6 працює з коробки — у прогонах №1–2 це
  був головний біль, тепер команда наведена прямо в гайді з поясненням обох поломок.
  Нове дрібне спостереження: у мене PR-и не виходять — пуш на GitHub заблокований
  політикою середовища (корп. машина). Оформлюю тижні локальними гілками w1…wN
  з merge; реальний PR + CI-гейт відкладаю до тижня 6 — там він обов'язковий.
- **скільки заходів знадобилось:** 1 на все (код зібрався з першої спроби).
- **чи дивився еталон:** ні.
- **суб'єктивно:** найсильніше місце тижня — «поломка реєстру видна» як окремий чек
  ментора: fail-visible дефолт без "support" — дизайн-рішення, яке я б сам не зробив;
  моя інтуїція була б якраз «розумний дефолт», і урок пояснює, чому це погано.

---

## Тиждень 2 · L03 + L04 (лонгріди + лаби) + hw2

- **що робив:** прочитав обидва уроки. L03-лаба: конфіг gateway (mock-mini/mock-strong на
  одному mock, drop_params, num_retries:0), `Route()` у сервісі, розподіл у БД, «третій
  провайдер на папері» (блок azure вже в YAML), fallback-порядок коментарем біля роутера.
  L04-лаба: прайс-словник (mini 0.00015/0.0006, strong 0.0025/0.01 за 1k), формула
  cost_usd з Math.Round(...,6) і null для невідомої моделі, `GET /cost` із сумою за
  сьогодні + budget_usd=5.0.
- **що вийшло (фактичні виводи):**
  - розподіл: mock-mini 5 / mock-strong 3 після змішаного трафіку;
  - `SELECT model, prompt_tokens, completion_tokens, cost_usd`: strong 17+9 → 0.000132,
    mini 18+11 → 0.000009 (~15x різниця на однаковій довжині);
  - питома ціна (чек ментора): strong 0.000005077 vs mini 0.000000310 за токен;
  - `GET /cost` → `{"today_usd":0.0001,"budget_usd":5}`;
  - evals 6/6 зелені.
- **де спіткнувся:** **кирилиця в curl** — класика, про яку гайд попереджає: перші
  українські ескалації («поверн», «терміново») пішли на mock-mini, бо Git Bash
  спотворив UTF-8 в аргументах і маркери не змачились. Помітив по дивному розподілу
  (1 strong замість 3), переслав `--data @file.json` — усе стало на місця. Це саме той
  «дефект, який ховає ASCII-only перевірка»: якби я слав тільки англійську, роутер
  виглядав би справним, а українські користувачі їхали б на дешевій моделі. Гайд
  (§5 і Troubleshooting) цю пастку описує, тож знахідка «працює як задокументовано»,
  але легко пропустити: попередження в §5 сформульоване про «перевірку української»,
  а вкусило мене в лабі роутингу.
- **скільки заходів знадобилось:** 2 (через кодування; сам код — з першого разу).
- **чи дивився еталон:** ні.
- **суб'єктивно:** зв'язка routing×cost «окупається на очах» — найкращий момент курсу
  поки що: різницю вартості видно в БД одним SELECT. Блок про «одне звернення ≠ один
  виклик» (ключ звернення ≠ ключ виклика) — річ, яку я б точно зробив неправильно
  в реальній системі.

---
